# Frontend environment variables (Vite)
# Copy to `.env` for local dev. In production (Render static site) set this in
# the dashboard to your deployed backend URL, e.g. https://cloud-ide-backend.onrender.com
VITE_API_URL=http://localhost:3000
